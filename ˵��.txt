ServiceSendMessage是绑定service，获得service内部类的实例，通过实例里的方法来向service发送数据handler实例或进行控制，service通过拿到的handler向activity发送消息。
ServiceSendMessage 是service里面写一个handler，然后把其实例传递给activity，activity用handler向service传递数据。
Active 是以bindService的方式绑定服务，用获取到的DownloadService.DownloadBinder实例启动服务，并在Activity中启动一个线程监听服务的进度信息， 及时的显示到按钮下方。
Passive 是service以broadCast的方式来向activity传递数据。
LocalServiceTest是用观察者模式，有serveice发消息给activity。